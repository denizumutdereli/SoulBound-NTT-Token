import { HardhatUserConfig } from "hardhat/types";

export const docgen:HardhatUserConfig["docgen"] =  {
    // path: './docs',
    // clear: true,
    // runOnCompile: true,
    // only:["AnyToken.sol"]
  }