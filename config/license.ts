import { HardhatUserConfig } from "hardhat/config";

export const spdxLicenseIdentifier:HardhatUserConfig["spdxLicenseIdentifier"] = {
    // overwrite: true,
    // runOnCompile: true,
    // except: ['vendor/']
  };